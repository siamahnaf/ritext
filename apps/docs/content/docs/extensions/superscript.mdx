---
title: Superscript
description: Toggle superscript formatting on selected text.
---

The `Superscript` extension adds superscript text formatting (e.g., x²). Supports `Ctrl+.` / `⌘+.`.

## Import

```tsx
import { Superscript } from 'ritext/extension/superscript';
```

## Usage

```tsx
const extensions = [Superscript];
```

## Options

<TypeTable
  type={{
    className: {
      description: 'CSS class for the toolbar button.',
      type: 'string',
    },
    icon: {
      description: 'Custom icon.',
      type: 'ReactNode',
    },
    activeClassName: {
      description: 'CSS class when superscript is active.',
      type: 'string',
    },
    tooltip: {
      description: 'Show tooltip on hover.',
      type: 'boolean',
      default: 'false',
    },
  }}
/>

## Keyboard Shortcut

| Action | Windows / Linux | macOS |
|--------|----------------|-------|
| Toggle Superscript | `Ctrl+.` | `⌘+.` |

<Callout type="info">
  For a combined sub/superscript dropdown button, use the [Sub & Superscript](/docs/extensions/sub-and-superscript) extension instead.
</Callout>
