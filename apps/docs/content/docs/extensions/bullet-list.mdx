---
title: Bullet List
description: Create unordered (bullet) lists.
---

The `BulletList` extension adds support for unordered lists. Requires `ListItem` from base extensions.

## Import

```tsx
import { BulletList } from 'ritext/extension/bulletlist';
```

## Requirements

```tsx
import { ListItem } from 'ritext/extension/base';

const extensions = [ListItem, BulletList];
```

## Usage

```tsx
const extensions = [ListItem, BulletList];
```

## Options

<TypeTable
  type={{
    className: {
      description: 'CSS class for the toolbar button.',
      type: 'string',
    },
    icon: {
      description: 'Custom icon.',
      type: 'ReactNode',
    },
    activeClassName: {
      description: 'CSS class when inside a bullet list.',
      type: 'string',
    },
    tooltip: {
      description: 'Show tooltip on hover.',
      type: 'boolean',
      default: 'false',
    },
  }}
/>

## Keyboard Shortcut

| Action | Windows / Linux | macOS |
|--------|----------------|-------|
| Toggle Bullet List | `Ctrl+Shift+8` | `⌘+Shift+8` |

## Nested Lists

Bullet lists support nesting. Use `Tab` to increase list depth and `Shift+Tab` to decrease it (requires `ListKeymap` from base extensions).
